<div class="bg-blue-500">
  <nav class="relative px-4 py-4 flex justify-between items-center bg-white">
    <a class="text-3xl font-bold leading-none" href="#">
      <!-- logo -->
      <svg class="h-10" viewBox="0 0 10240 10240">
        <path
          xmlns="http://www.w3.org/2000/svg"
          d="M8284 9162 c-2 -207 -55 -427 -161 -667 -147 -333-404-644-733-886 …"
        ></path>
      </svg>
    </a>
    <div class="lg:hidden">
      <button class="navbar-burger flex items-center text-blue-600 p-3">
        <svg
          class="block h-4 w-4 fill-current"
          viewBox="0 0 20 20"
          xmlns="http://www.w3.org/2000/svg"
        >
          <title>Mobile menu</title>
          <path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"></path>
        </svg>
      </button>
    </div>

    <!-- menú desktop -->
    <ul
      class="hidden absolute top-1/2 left-1/2 transform -translate-y-1/2 -translate-x-1/2 lg:mx-auto lg:flex lg:items-center lg:w-auto lg:space-x-6"
    >
      <li>
        <a class="text-sm text-gray-400 hover:text-purple-900" href="/"
          >Inicio</a>
      </li>
      <li class="text-gray-300">…</li>
      <li>
        <a class="text-sm text-gray-400 hover:text-purple-900" href="/#about"
          >¿Quienes somos?</a>
      </li>
      <li class="text-gray-300">…</li>
      <li>
        <a class="text-sm text-gray-400 hover:text-purple-900" href="/#services"
          >Servicios</a>
      </li>
      <li class="text-gray-300">…</li>
      <li>
        <a class="text-sm text-gray-400 hover:text-purple-900" href="/#contacto"
          >Contacto</a>
      </li>
    </ul>
  </nav>

  <!-- menú mobile -->
  <div class="navbar-menu relative z-50 hidden">
    <div class="navbar-backdrop fixed inset-0 bg-gray-800 opacity-25"></div>
    <nav
      class="fixed top-0 left-0 bottom-0 flex flex-col w-5/6 max-w-sm py-6 px-6 bg-white border-r overflow-y-auto"
    >
      <div class="flex items-center mb-8">
        <a class="mr-auto text-3xl font-bold leading-none" href="#">
          <svg class="h-12" viewBox="0 0 10240 10240">
            <path d="M8284 9162 c-2 -207 -55 -427 …"></path>
          </svg>
        </a>
        <button class="navbar-close">
          <svg
            class="h-6 w-6 text-gray-400 cursor-pointer hover:text-gray-500"
            xmlns="http://www.w3.org/2000/svg"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        </button>
      </div>

      <div>
        <ul>
          <li class="mb-1">
            <a
              class="mobile-link block p-4 text-sm font-semibold text-gray-400 hover:text-purple-900 rounded"
              href="/">Inicio</a>
          </li>
          <li class="mb-1">
            <a
              class="mobile-link block p-4 text-sm font-semibold text-gray-400 hover:text-purple-900 rounded"
              href="/#about">¿Quienes somos?</a>
          </li>
          <li class="mb-1">
            <a
              class="mobile-link block p-4 text-sm font-semibold text-gray-400 hover:text-purple-900 rounded"
              href="/#services">Servicios</a>
          </li>
          <li class="mb-1">
            <a
              class="mobile-link block p-4 text-sm font-semibold text-gray-400 hover:text-purple-900 rounded"
              href="/#contacto">Contacto</a>
          </li>
        </ul>
      </div>

      <div class="mt-auto">
        <div class="pt-6"></div>
        <p class="my-4 text-xs text-center text-gray-400">
          <span>Copyright Bee Labs 🐝 © 2025</span>
        </p>
      </div>
    </nav>
  </div>
</div>

<script>
  // Burger menus
  document.addEventListener("DOMContentLoaded", function () {
    const burger = document.querySelectorAll(".navbar-burger");
    const menu = document.querySelectorAll(".navbar-menu");
    const close = document.querySelectorAll(".navbar-close");
    const backdrop = document.querySelectorAll(".navbar-backdrop");
    const links = document.querySelectorAll(".mobile-link"); // 👈 enlaces del menú móvil

    function toggleMenu() {
      menu.forEach((m) => m.classList.toggle("hidden"));
    }

    burger.forEach((b) => b.addEventListener("click", toggleMenu));
    close.forEach((c) => c.addEventListener("click", toggleMenu));
    backdrop.forEach((b) => b.addEventListener("click", toggleMenu));

    // 👉 cerrar menú al hacer clic en un enlace
    links.forEach((link) => link.addEventListener("click", toggleMenu));
  });
</script>
